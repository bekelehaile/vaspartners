"use client";

import { ServicesCatalog } from "@/components/ServicesCatalog";

/** Homepage services section — same collapsible catalog as the portal. */
export function LandingServicesSection() {
  return <ServicesCatalog />;
}
